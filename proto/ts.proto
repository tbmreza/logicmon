syntax = "proto3";
package ts;

// Zer the analyzer trait.
service Zer {
    rpc Replace (Source) returns (Reply);
}

message Source {
   string val = 1;
}

message Reply {
   int32 val = 1;
}

message InputEdit {
    string message = 1;
}
